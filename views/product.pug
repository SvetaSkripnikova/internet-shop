extends layout.pug

block title
    title PRODUCT CARD |  MILLION FLOWERS
block content
    body
        div(id="container")
            div(class="product-details")
                h1 #{title}
                br
                p -- #{category} --
                br
                br
                p(class="information") #{disc}
                    div(class="control")
                        button(class="mmybtn" onclick="location = 'http://localhost:3000/'")
                            span(class="price") #{price}₽
                            span(class="buy") На главную
            div(class="product-image")
                img(src=`${img}`)